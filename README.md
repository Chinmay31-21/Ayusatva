
  # Hospital Management Platform

  This is a code bundle for Hospital Management Platform. The original project is available at https://www.figma.com/design/48kF5e347i1drBerp0jOnV/Hospital-Management-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  